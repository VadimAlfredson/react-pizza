{/*
export const usePaginator = (items, currentPage= 1, countToPage = 8) => {
  const totalItemsCount = items.length
  const totalCount = Math.ceil(totalItemsCount / countToPage)
  const itemsToPage = items.slice((currentPage - 1) * countToPage, (currentPage - 1) * countToPage + countToPage > totalItemsCount ? totalItemsCount : (currentPage - 1) * countToPage + countToPage)

  debugger
  return [itemsToPage, totalCount]*/
}