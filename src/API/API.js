/*
import axios from "axios";
axios.get(`https://65d37906522627d50108f9e4.mockapi.io/pizzas?p=${currentPage + 1}&l=${4}&${category ? `category=${category}` : ''}&${sort ? `sortBy=${sort}` : ''}&${order ? `order=${order}` : ''}&${search ? `search=${search}` : ''}`)
    .then((response) => {
        console.log(response.status)
        return response.json()
    })
    .then(
        (json) => {
            setPizzas(json)
            setIsLoading(false)
        }
    )*/
